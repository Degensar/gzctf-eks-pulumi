{
  "name": "gzctf-eks",
  "version": "0.1.0",
  "private": true,
  "description": "Pulumi IaC that provisions an AWS EKS cluster and supporting resources to host the GZCTF capture-the-flag platform",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@pulumi/aws": "^7.34.0",
    "@pulumi/awsx": "^3.6.0",
    "@pulumi/eks": "^4.2.0",
    "@pulumi/kubernetes": "^4.32.0",
    "@pulumi/pulumi": "^3.247.0",
    "@pulumi/random": "^4.18.0"
  }
}
